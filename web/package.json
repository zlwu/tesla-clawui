{
  "name": "@tesla-openclaw/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint 'src/**/*.ts'",
    "test": "vitest run"
  },
  "dependencies": {
    "@tesla-openclaw/shared": "0.1.0"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "vite": "^6.2.1"
  }
}
